import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {
  // Todo: Property that not in use, with correct TypeScript config will give an error
  title = 'eagle6-test-app';
}
